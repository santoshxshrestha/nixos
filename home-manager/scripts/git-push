#!/usr/bin/env bash
if [ $# -eq 0 ]; then
    echo "Usage: C <commit message>"
    echo "Example: C fix login bug"
    exit 1
fi

MESSAGE="$*"
git add .
git commit -m "$MESSAGE"
git push origin "$(git branch --show-current)"
